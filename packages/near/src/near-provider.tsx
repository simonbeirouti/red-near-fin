'use client'

import '@near-wallet-selector/modal-ui/styles.css'

export function NearProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>  // Add NEAR provider logic here when needed
}